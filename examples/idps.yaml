---
states:
  - name: "eth0"
    interface: "eth0"
    transitions:
      - name: "default-traffic-to-idps"
        action:
          next-state: "dummy0"
          queue: "ingress"
        default: true
        queue: "ingress"
  - name: "dummy0"
    interface: "dummy0"
  - name: "dummy1"
    interface: "dummy1"
    transitions:
      - name: "default-traffic-to-eth0"
        action:
          next-state: "eth0"
          queue: "egress"
        default: true
        queue: "egress"